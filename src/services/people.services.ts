class PeopleService {
    public create(p : People){
        // Push new user to DB
    }

    public findById(id: Number){
        // Find user by ID
    }

    public update(p: People) {
        // Update given user
    }
}